import { http } from "../../api/config"

export const courseSlice = (value, history) => {
    return async (dispath) => {
        try {
            let result = await http.get
        } catch (error) {
            
        }
    }
}